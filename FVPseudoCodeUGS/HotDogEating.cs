using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BasicController : MonoBehaviour
{
    //Rigidbody2D rb;
    //hotDogCounter[];
    //public GameObject Hotdog;
    //public GameObject Drink;
    //public float Timer;
        
    void Awake()
    {
        //hotDogCounter[] = 0
        //Timer = 0
    }

    void Start()
    {
        //Controllers Space = eat Hotdog, A = Drink, D = Burp?
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    //Hunger meter divided in 5 (red 1 - green 5)
    //if meter == 1 {Throw up} Game over, red card
    //if meter == 2 {Messy animation}, yellow card, if yellow card x2, game over
    //if meter == 3 {Small mess animation}
    //if meter == 4 
    //if meter == 5
}
