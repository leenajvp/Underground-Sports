using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BasicController : MonoBehaviour
{
    //Rigidbody2D rb;
    //public maxSpinSpeed;
    //private SpinSpeed = 0,1;
    //private throwDistance = 0,1
        
    void Start()
    {
        //Spin speed
    }

    // Update is called once per frame
    void Update()
    {
        //Controller Space = Spin, D = Throw
        // if space key down spin * 0.1
        
    }
    //Spin speed meter
    //if spin speed > 2 meter is green
    //if spin speed > 1 meter is yellow
    //else meter is red

    //Throw distance
    //if meter is red random distance between 1-10 meters
    //if meter is yellow random distance between 11-20 meters
    //if meter is green random distance between 21-30 meters

    //Animation for speed stages
}
